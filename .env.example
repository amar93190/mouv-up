VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_MAKE_WEBHOOK_URL=https://hook.eu1.make.com/your-webhook-id
# Optional (dev only): enable service worker + install prompt in local development
# VITE_ENABLE_PWA_DEV=true

# Important:
# - Never put sb_secret_* keys in this frontend project.
# - Keep your real values in .env only (do not commit .env).
